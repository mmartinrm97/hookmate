# ---------------------------------------------------------------------------
# S3 backend configuration is defined in versions.tf (terraform.backend block).
# See versions.tf for backend setup instructions.
#
# This file exists to document the backend pattern and provide a single place
# to update the backend config when needed.
# ---------------------------------------------------------------------------
